require 'printer'

describe Printer do 

  # let(:printer) { Printer.new }
  # let(:mockAccount) { double: account, :statement => [["Date", "Amount", "Balance"]]}

  # it { is_expected.to respond_to(:print_statement).with(1).argument }

  # it "should print out statement line-by-line" do
    
  # end

end